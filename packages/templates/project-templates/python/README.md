# Template python

Template mínimo com estrutura moderna, `pytest` para testes e `ruff` para lint/format.

## Comandos
- `python -m pip install -e .[dev]`
- `pytest`
- `ruff check .`
- `ruff format .`
